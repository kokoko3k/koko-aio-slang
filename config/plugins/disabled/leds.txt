// Emulate 2 power leds with an halo.
// led x hue: the color of the led
// X,Y the position on screen
// leds are drawn over the foreground image, which needs to be enabled.
//
// To use:
// 1 - Have a transparent foreground png overlay image setted up; leds in the png should appear as powered on.
// 2 - With a gfx program, lower the alpha channel of the pixel that correspond to leds, make it "just almost" zero so that colors informations are preserved, and save.
// 3 - In koko-aio parameters, set x and y values of each led so that they correspond to the real led position.
// 4 - tweak hue so that it matches the led color of the png.


#ifndef USE_LEDS
#define USE_LEDS
#endif

//denise drive led support
#pragma parameter_denise autoEmu_driveLED "what here?" 1 0 1 1

#pragma parameter LED_LABEL1            "      == PLUGIN: LEDS =="	0.0 0.0 0.0 1.0
#pragma parameter LED_SIZE	            "      Power leds halo size (0 to disable) (Image over only)"	0.5  0.0  3.0    0.01
#pragma parameter LED_SHAPE             "      Power leds halo shape (circle to square)"                0.0  0.0  0.5    0.01
#pragma parameter LED_ASPECT            "      Power leds halo Aspect"                                  1.0  0.2  5.0    0.05
#pragma parameter LED_EXPOSURE          "      Power leds Overexposure"                                 0.5  0.0  2.0    0.1
#pragma parameter LED1_HUE	            "         Power led 1 hue"   1.0   0.0  1.0    0.01
#pragma parameter LED1_X	            "         Power led 1 X"	  0.1  -1.0  2.0   0.001
#pragma parameter LED1_Y	            "         Power led 1 Y"	  0.1  -1.0  2.0   0.001
#pragma parameter_denise LED2_IS_DRIVE  "         Power led 2 is bound to drive led"   1 0 1 1
#pragma parameter LED2_HUE	            "         Power led 2 hue"   0.3   0.0  1.0    0.01
#pragma parameter LED2_X	            "         Power led 2 X"	  0.1  -1.0  2.0   0.001
#pragma parameter LED2_Y	            "         Power led 2 Y"	  0.2  -1.0  2.0   0.001
#pragma parameter LED_LABEL2            "      =="	0.0 0.0 0.0 1.0
